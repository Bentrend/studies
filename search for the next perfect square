let sq = 120;
function findNextSquare(sq) {
  if (Number.isInteger(Math.sqrt(sq))) {
    sq = sq + 1;
    while (!Number.isInteger(Math.sqrt(sq))) {
      sq++;
    }
  } else {
    return sq;
  }
}
findNextSquare(sq);
